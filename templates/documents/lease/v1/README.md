# lease/v1

This template version is rendered directly by
`src/lib/contract-generation/render.ts`. It deliberately has no HTML or
browser dependency. The renderer preserves the versioned lease wording and
writes a searchable A4 PDF using built-in Helvetica fonts. Its layout and
24-section assured-periodic-tenancy wording are distilled from
`Tenancy_Agreement_322AlumRockRoad_Updated_2026.docx`.
