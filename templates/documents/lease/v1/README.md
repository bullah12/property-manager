# lease/v1

This template version is rendered directly by
`src/lib/contract-generation/render.ts`. It deliberately has no HTML or
browser dependency. The renderer preserves the versioned lease wording and
writes a searchable A4 PDF using built-in PDF fonts.
